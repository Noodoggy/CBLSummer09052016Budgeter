@model CBLSummer09052016Budgeter.Models.CodeFirst.ViewModels.InviteViewModel

<div class="col-md-6 col-md-offset-3">
    @*<div class="hpanel plan-box hgreen active">
        <div class="panel-heading hbuilt text-center">*@
            <h4 class="font-bold">Household Name:  @Model.HouseholdName</h4>
        @*</div>
        <div class="panel-body">
            <p class="text-muted" style="text-align:center">
                double click user to add or remove from project
            </p>*@
            <div class="row">
                <div class="col-sm-12">
                    <div class="row">
                        Add this user?

                            @using (Html.BeginForm("Invite", "Households", FormMethod.Post))
                            {

                                @Html.AntiForgeryToken()
                                @Html.HiddenFor(model => model.HouseId)
                                @*<input type="hidden" name="model.AddToHousehold" id="model.AddToHousehold" value="@Model.AddToHousehold" />*@

                                            @Html.HiddenFor(model => Model.UserId)
                                            @*<input type="hidden" name="model.UserToAdd" id="model.UserToAdd" value="@Model.UserToAdd" />*@

                                            @Html.DisplayFor(model => model.UserToAddName)

                                            <br />
                                            @Html.ValidationSummary(true, "", new { @class = "text-danger" })
                                            <div class="row">
                                                <div class="col-sm-12">
                                                   
                                                        <div class="col-md-12">
                                                            <input type="submit" value="Save" class="btn btn-success" />
                                                        </div>
                                                  
                                                </div>
                                            </div>


                            }
                        </div>
                    </div>
                </div>
            </div>

